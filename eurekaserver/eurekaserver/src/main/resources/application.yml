spring:
  application:
    name: mseurekaserver
  security:
    basic:
      enabled: true
    user:
      name: eureka-user
      password: abc123

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false